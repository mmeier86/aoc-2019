/**
 * @file day_{{DAY}}.cpp
 * @brief Main file for Day {{DAY}}
 */

#include "{{NAME}}.hpp"

#include <aoc_except.hpp>
#include <aoc_main.hpp>

#include <iostream>

int main(int argc, char** argv){
  try{
    std::cout << AoC::common::aoc_main<DayClass>(argc,argv) << std::endl;
  } catch(const AoC::common::AoCError& e){
    std::cerr << e.what() << std::endl;
    return 1;
  }
  return 0;
}
