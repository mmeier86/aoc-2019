##
# Build config for Advent of Code 2019 Day 01
##

cmake_minimum_required(VERSION 3.15)

project("Advent of Code Day {{DAY}}"
  LANGUAGES CXX
  )

include(${CMAKE_CURRENT_LIST_DIR}/../cmake/day.cmake)

init_aoc_day(DAY "{{DAY}}" NAME "{{NAME}}")
